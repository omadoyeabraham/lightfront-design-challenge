/**
 * The string keys here are unique keys to be used as query/mutation keys with the React-Query Library
 */

enum API_QUERIES {
  AMAZON_PAGE_RANK = "amazon_page_rank",
}

export default API_QUERIES;
